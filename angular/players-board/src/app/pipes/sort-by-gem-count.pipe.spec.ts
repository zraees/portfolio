import { SortByGemCountPipe } from './sort-by-gem-count.pipe';

describe('SortByGemCountPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByGemCountPipe();
    expect(pipe).toBeTruthy();
  });
});
