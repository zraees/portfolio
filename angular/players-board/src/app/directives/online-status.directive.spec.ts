import { OnlineStatusDirective } from './online-status.directive';

describe('OnlineStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new OnlineStatusDirective();
    expect(directive).toBeTruthy();
  });
});
